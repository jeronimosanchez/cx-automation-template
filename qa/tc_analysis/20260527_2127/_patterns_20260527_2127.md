---
run_ts: 20260527_2127
generado: 2026-05-28
---

TCs analizados en este batch: TC-URGENCIA-01, TC-URGENCIA-03

## Patrón URGENCIA-IGNORADA — el agente ignora restricciones temporales del usuario

**TCs:** TC-URGENCIA-01, TC-URGENCIA-03

| Razón | Evaluación | Detalle |
|---|---|---|
| Síntoma compartido | ✅ | Ambos TCs: user introduce restricción temporal explícita (hora exacta o día) → agente la trata como contexto irrelevante y procede con catálogo/exploración estándar |
| Capa compartida | ✅ | Capa Comportamiento (compra.yaml sin sub-flujo URGENCIA) en ambos. Adicionalmente: capa Histórico también compartida — mismo commit histórico cubría ambos casos |
| Fix compartido | ✅ | Cherry-pick de `a10ab02` (bloque DETECCION URGENCIA TEMPORAL) cierra ambos TCs con un único cambio |

### Causa raíz común

El playbook `compra.yaml` carece de un sub-flujo que detecte triggers temporales en la intención inicial del usuario (`hoy`, `mañana`, `este viernes`, formato `HH:MM`, etc.) ANTES de proceder con el flujo estándar de catálogo. El orquestador (línea 254 de `petal_cx_orchestrator.yaml`, regla ZG-5) solo deriva a G1(business) cuando el usuario PREGUNTA plazo, no cuando lo MENCIONA dentro de una intención de compra.

El Sheet `business` contiene todos los datos necesarios (`horario_corte_mismo_dia: 14:00`, `tiempo_entrega_estimado: 24h en Madrid y Barcelona, resto de ciudades 48 horas`, `envio_madrid_ciudad: Mismo dia (pedidos antes 14:00)`). El bug es 100% de instrucción al LLM: la información está, no se expone.

### Historia de regresión

`git log -n 20 -- definitions/playbooks/compra.yaml` revela un patrón de **3 ciclos fix→revert** para este mismo bloque:

| Commit fix | Commit revert | Asunto |
|---|---|---|
| `3f041df` | `aca187c` | CASO ESPECIAL urgencia/plazo (TC-URGENCIA-01) |
| `a10ab02` | `2126c52` | bloque DETECCION URGENCIA TEMPORAL al FLUJO PRINCIPAL (Sol#1) |
| `3e0b2d1` | `1f95cae` | mismo fix, 2º intento |

Patrón "demo break": el fix se aplica, se valida, y se revierte para mantener el TC reproducible en demos. Esto es valioso como evidencia: **el bug es conocido, el fix es conocido, el código está versionado** — solo falta decidir cuándo dejarlo permanente.

### ROI del fix compartido

- **Coste**: ~25 min (cherry-pick + verificación + QA)
- **TCs cerrados**: 2 (TC-URGENCIA-01 + TC-URGENCIA-03)
- **TCs/hora**: ~4.8

## Recomendación

### Acción inmediata sugerida

1. **Cherry-pick `a10ab02`** sobre rama feature limpia: `git cherry-pick a10ab02`
2. **Verificar bloque**: que el bloque DETECCION URGENCIA TEMPORAL aparece tras el GOAL en `compra.yaml`
3. **Push + deploy** (~3 min)
4. **Re-ejecutar QA** filtrando TC-URGENCIA-01 + TC-URGENCIA-03 con `--runs 3`
5. **Esperado**: 6/6 PASS

### Decisión post-demo

Tras la demo, decidir si el fix `a10ab02` se hace permanente en `main` o si se mantiene la dinámica break-for-demo. Recomendación: dado que el ciclo fix→revert se ha repetido 3 veces, formalizar el fix permanente + extraer un TC alternativo "no-urgencia" para demos futuras evita re-romper el playbook cada vez.

### Parámetros / slots requeridos entre playbooks

| Slot | Playbook origen | Playbook destino | Obligatorio | Notas |
|------|----------------|-----------------|-------------|-------|
| `$fecha_entrega` | Compra | Checkout | Condicional | Solo si Solución #2 (opcional). Permite validación de viabilidad en Checkout. |
| `$hora_entrega` | Compra | Checkout | Condicional | Idem |
| `$urgencia_detectada` | Compra | Checkout | No | Flag de telemetría. No bloquea flujo. |
