---
run_ts: 20260527_151044
generado: 2026-05-27
---

TCs analizados en este batch: TC-MULTI-PRODUCTO-01, TC-URGENCIA-01, TC-URGENCIA-03

## Patrón URGENCIA-PLAZO — agente ignora marcadores temporales del usuario

**TCs:** TC-URGENCIA-01, TC-URGENCIA-03

| Razón | Evaluación | Detalle |
|---|---|---|
| **Síntoma compartido** | ✅ | Ambos TCs presentan al agente una marca temporal en T1 ("hoy a las 18:00" / "este viernes") y en los 3 runs de cada uno el agente la ignora, la ecoa sin acción o (run 3 de URGENCIA-01) inventa una promesa imposible. Ningún run menciona plazo, política de envíos ni viabilidad. |
| **Capa compartida** | ✅ | En ambos análisis la 🔴 Capa Comportamiento es la causa raíz: `compra.yaml` (v39, 860 líneas) no contiene ningún bloque que detecte expresiones de plazo/fecha (`grep "URGENCIA\|24h\|plazo\|fecha"` solo devuelve menciones inertes en ejemplos). El playbook entra directo al flujo estándar sin gate temporal previo. |
| **Fix compartido** | ✅ | Un único bloque "DETECCIÓN PLAZO/FECHA" insertado al inicio del FLUJO PRINCIPAL de `compra.yaml` cubre las dos variantes: expresiones absolutas (hora exacta) y relativas (día de la semana). Mismo archivo, misma sección, mismo commit. |

### ROI del patrón

| Fix | TCs resueltos | Esfuerzo | ROI |
|---|---|---|---|
| Bloque DETECCIÓN PLAZO/FECHA en `compra.yaml` (cubre absolutas + relativas, con caso Madrid<14:00 explícito para evitar el cuarto ping-pong del historial `3f041df→aca187c→3e0b2d1→1f95cae→a10ab02→2126c52`) | **2 TCs** | ~15 min | 8 TCs/h |

**Recomendación de secuencia:** fijar primero TC-URGENCIA-01 (es el análisis con mejor instrumentación — incluye la corrección de la premisa errónea "Petal no soporta same-day" y el caso Madrid<14:00 explícito). El rerun debe ejecutar TC-URGENCIA-01 + TC-URGENCIA-03 a la vez: si la causa raíz es realmente común, ambos pasan con el mismo deploy y no hace falta un segundo PR.

## TCs sin patrón

| TC | Por qué no forma patrón |
|---|---|
| TC-MULTI-PRODUCTO-01 | Bug independiente del template del Paso 7 multi-producto en `compra.yaml`, introducido a propósito por el commit `c9ce667` ("DEMO BREAK v2") y nunca revertido. Fix: `git revert c9ce667` (~5 min). No comparte síntoma (ausencia de total combinado en ECO RESUMEN, no ignorar marca temporal), ni capa raíz dominante (🔴 Histórico + 🔴 Comportamiento por template roto, no por bloque ausente), ni fix con los TCs de urgencia. |

## Resumen ejecutivo

1 patrón detectado (URGENCIA-PLAZO) que afecta a 2 de los 3 TCs analizados. 1 TC con bug independiente (MULTI-PRODUCTO). Orden recomendado: aplicar primero el fix del patrón URGENCIA-PLAZO (mayor ROI: 2 TCs cerrados con 1 commit de ~15 min → 8 TCs/h), y en paralelo o a continuación revertir `c9ce667` para cerrar TC-MULTI-PRODUCTO-01 (~5 min). Deuda técnica detectada: la política de plazos del Sheet (`horario_corte_mismo_dia=14:00`, `tiempo_entrega_estimado=24h Madrid/Barcelona, 48h resto`) está huérfana — ningún playbook la consulta. La Solución #2 del análisis de TC-URGENCIA-01 (hardcoded) es el fix pragmático ahora; la Solución #1 (consulta al Sheet) queda para sprint posterior para conectar el dato real y cerrar la deuda.
