---
run_ts: 20260701_170805
generado: 2026-07-01
---

TCs analizados en este batch: TC-C35, TC-DECO-02, TC-BODA-01, TC-FRUSTRACION-01, TC-URGENCIA-03, TC-FRUSTRACION-LEX-01, TC-TONO-CAMBIO-SOLEMNE-01, TC-TONO-APLICACION-CORP-01

## Patrón DOBLE-RESPUESTA — Orquestador emite turno antes de transferir a Compra

**TCs:** TC-BODA-01, TC-TONO-CAMBIO-SOLEMNE-01

| Razón | Evaluación | Detalle |
|---|---|---|
| **Síntoma compartido** | ✅ | Respuesta concatenada con `\n` — Orquestador emite mensaje Y Compra emite el suyo en el mismo turno de usuario |
| **Capa compartida** | ✅ | Bug Arquitectura / Playbook en `petal_cx_orchestrator.yaml` — instrucción de transición invisible no prohíbe explícitamente emitir respuesta emocional previa |
| **Fix compartido** | ✅ | Añadir en G5 y G3 del Orquestador: `⛔ NO emitas ningún mensaje al usuario antes de transferir, aunque $registro=celebracion o solemne. La transferencia es SILENCIOSA.` |

### ROI del patrón

| Fix | TCs resueltos | Esfuerzo | ROI |
|---|---|---|---|
| Añadir regla de silencio en G5 + G3 del Orquestador (`petal_cx_orchestrator.yaml`) | **2 TCs** (parcialmente — elimina doble respuesta) | ~15 min | 8 TCs/h |

**Recomendación de secuencia:** Aplicar primero los fixes de test (Patrón CHECK-MAL-CALIBRADO) para que el runner refleje el estado real. Luego abordar el fix del Orquestador como tarea separada con rerun de suite completa por su impacto transversal.

---

## Patrón CHECK-MAL-CALIBRADO — Regex de test no captura la respuesta real del agente

**TCs:** TC-C35, TC-FRUSTRACION-LEX-01, TC-TONO-CAMBIO-SOLEMNE-01, TC-TONO-APLICACION-CORP-01

| Razón | Evaluación | Detalle |
|---|---|---|
| **Síntoma compartido** | ✅ | El agente responde correctamente (o de forma semánticamente válida) pero el check regex no lo detecta — FAIL espurio en el runner |
| **Capa compartida** | ✅ | Capa Test — patrones regex incompletos, truncados o con scope incorrecto en los archivos YAML de TC |
| **Fix compartido** | ✅ | Ampliar/corregir los patrones en `qap/tc_1_0.yaml` y `qap/tc_1_1.yaml` |

Casos concretos:
- **TC-C35** (`tc_1_1.yaml`): patrón `ocasion|celebra|...` asume pregunta de ocasión pero el agente pregunta flor/color (comportamiento correcto dado `ocasion_detectada` ya tiene valor). Fix: cambiar a `flor|color|mente|tipo|producto|tenias`.
- **TC-FRUSTRACION-LEX-01** (`tc_1_0.yaml`): patrón `entend` no coincide con `Entiendo` (raíz distinta: `entend` vs `entien`). Además falta `lament` para capturar `Lamento`. Fix: sustituir `entend` por `entien` y añadir `lament|sentim`.
- **TC-TONO-CAMBIO-SOLEMNE-01** (`tc_1_0.yaml`): patrón `funebr` no captura `funeral` (raíces distintas). Falta `lament`. Patrón `opciones` nunca aparece en la respuesta real. Fix: añadir `lament|funeral`, eliminar `opciones` y `sobri`.
- **TC-TONO-APLICACION-CORP-01** (`tc_1_0.yaml`): `not_expected: [mira,]` penaliza conector explícitamente permitido para `$registro=estandar`. Fix: inyectar `session_params: {registro: solemne}` o eliminar `mira,` del `not_expected`.

### ROI del patrón

| Fix | TCs resueltos | Esfuerzo | ROI |
|---|---|---|---|
| Recalibrar 4 checks en `tc_1_0.yaml` + `tc_1_1.yaml` | **4 TCs** | ~20 min total | 12 TCs/h |

**Recomendación de secuencia:** Prioridad máxima — son fixes de test sin deploy CI/CD. Aplicar todos en una sola rama antes de abordar fixes de playbook.

---

## Patrón EMOJI-CONTEXTO — Emoji 🌸 aparece en contexto donde está prohibido

**TCs:** TC-BODA-01, TC-TONO-CAMBIO-SOLEMNE-01

| Razón | Evaluación | Detalle |
|---|---|---|
| **Síntoma compartido** | ✅ | El emoji 🌸 aparece en la respuesta y el `not_expected` lo detecta correctamente |
| **Capa compartida** | ⚠️ | La fuente del 🌸 es la doble respuesta del Orquestador (que no tiene tabla de emoji tan explícita como Compra) — no es bug de instrucción de emoji de Compra per se |
| **Fix compartido** | ⚠️ | El fix primario es el del Patrón DOBLE-RESPUESTA; el 🌸 desaparece cuando el Orquestador deja de emitir turno en G3/G5 |

Nota: En TC-BODA-01 el `not_expected: [🌸]` es además mal calibrado para `$registro=celebracion` (donde 🌸 está permitido por instrucción). El fix de test (ajustar scope del `not_expected` a solo `solemne`) es necesario además del fix arquitectónico.

### ROI del patrón

| Fix | TCs resueltos | Esfuerzo | ROI |
|---|---|---|---|
| Fix Orquestador (ver Patrón DOBLE-RESPUESTA) + ajustar scope not_expected en TC-BODA-01 | **2 TCs** | ~20 min | 6 TCs/h |

**Recomendación de secuencia:** Dependiente del Patrón DOBLE-RESPUESTA. Resolver en el mismo PR que el fix del Orquestador.

---

## Patrón INSTRUCCION-DEBILITADA — Refactor eliminó antipatrón explícito que bloqueaba comportamiento incorrecto

**TCs:** TC-URGENCIA-03, TC-TONO-CAMBIO-SOLEMNE-01

| Razón | Evaluación | Detalle |
|---|---|---|
| **Síntoma compartido** | ✅ | El bloque de instrucción relevante existe en el playbook pero el LLM lo interpreta de forma incompleta — omite acción obligatoria o la verbaliza cuando debía ser silenciosa |
| **Capa compartida** | ✅ | Bug Playbook en `compra.yaml` — instrucciones sin antipatrón explícito (`NUNCA hagas X`) permiten al LLM tomar atajos |
| **Fix compartido** | ✅ | Restaurar/añadir frases antipatrón con `⛔` y ejemplos de lo que NO hacer en las secciones DETECCION RESTRICCION TEMPORAL y VIGILANCIA DE CAMBIO |

Casos concretos:
- **TC-URGENCIA-03**: commit `aa05036` eliminó la frase `NUNCA saltes al catalogo ni al slot-filling sin haber informado primero la politica de envio` del bloque DETECCION RESTRICCION TEMPORAL. El agente hace ECO del marcador temporal pero salta al slot-filling. Fix: restaurar esa frase.
- **TC-TONO-CAMBIO-SOLEMNE-01**: la instrucción `No anuncies el cambio al usuario` en VIGILANCIA DE CAMBIO no tiene `⛔` ni ejemplo prohibido. El LLM verbaliza `"el registro cambia a solemne"`. Fix: reforzar con `⛔ NUNCA digas al usuario 'el registro cambia a solemne' ni ninguna variante`.

### ROI del patrón

| Fix | TCs resueltos | Esfuerzo | ROI |
|---|---|---|---|
| Restaurar frase antipatrón en DETECCION RESTRICCION TEMPORAL + reforzar VIGILANCIA DE CAMBIO (`compra.yaml`) | **2 TCs** | ~15 min | 8 TCs/h |

**Recomendación de secuencia:** Aplicar tras los fixes de test. Requiere deploy CI/CD. Verificar con rerun de TC-URGENCIA-01 + TC-URGENCIA-03 (comparten el mismo bloque) y TC-TONO-CAMBIO-SOLEMNE-01.

---

## TCs sin patrón

| TC | Por qué no forma patrón |
|---|---|
| TC-DECO-02 | Doble causa independiente (Bug Playbook: formato `--` no seguido + Bug Test: regex no cubre `€`). El Bug de formato es específico del bloque FORMATO AL USUARIO de `compra.yaml`; el commit `aa05036` añadió la instrucción pero el agente aún no la sigue con consistencia. No comparte raíz con otros TCs del batch. |
| TC-FRUSTRACION-01 | Bug Test primario (TC mal dimensionado: 4 turnos no alcanzan umbral de rechazo-4) + Bug Playbook secundario (inicio del contador de rechazos no definido). Causa específica de este TC — no comparte patrón estructural con otros TCs del batch. |

---

## Resumen ejecutivo

**4 patrones detectados** en 8 TCs:

| Patrón | TCs | Tipo | Prioridad |
|---|---|---|---|
| CHECK-MAL-CALIBRADO | TC-C35, TC-FRUSTRACION-LEX-01, TC-TONO-CAMBIO-SOLEMNE-01, TC-TONO-APLICACION-CORP-01 | Test | P0 — sin deploy |
| INSTRUCCION-DEBILITADA | TC-URGENCIA-03, TC-TONO-CAMBIO-SOLEMNE-01 | Playbook | P1 — deploy requerido |
| DOBLE-RESPUESTA | TC-BODA-01, TC-TONO-CAMBIO-SOLEMNE-01 | Arquitectura | P2 — impacto transversal, rerun suite |
| EMOJI-CONTEXTO | TC-BODA-01, TC-TONO-CAMBIO-SOLEMNE-01 | Mixto (Test + Arquitectura) | P2 — dependiente de DOBLE-RESPUESTA |

**Orden de ejecución recomendado:**
1. Fixes CHECK-MAL-CALIBRADO (4 TCs, ~20 min, sin deploy) — máximo ROI inmediato.
2. Fixes INSTRUCCION-DEBILITADA (2 TCs, ~15 min + deploy) — cierra bugs de comportamiento real.
3. Fix DOBLE-RESPUESTA + EMOJI-CONTEXTO (2 TCs, ~30 min + rerun suite completa) — requiere gate por impacto en G3/G5 global.

**Deuda técnica identificada:** TC-TONO-CAMBIO-SOLEMNE-01 aparece en 3 patrones simultáneamente (el TC más complejo del batch). La lista DUELO de Compra no incluye `tanatorio` ni `ha fallecido` — alineación pendiente con la lista del Orquestador. TC-DECO-02 y TC-FRUSTRACION-01 tienen causas aisladas que requieren tratamiento individual.
