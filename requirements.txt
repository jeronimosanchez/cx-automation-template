# Python deps para cx-automation-template.
# Promptfoo NO esta aqui (legacy archivado en qap/_archive/, ver qap/_archive/README.md).

# Runtime
requests>=2.31
google-auth>=2.23
pyyaml>=6.0

# Dev / tests (Sprint 2, Task 7). Sin red, sin auth.
pytest>=7.0

# Servidor local que expone el pipeline al panel (act/server.py)
flask>=2.2
flask-cors>=4.0

# Pipeline Cloud Run (act/*_cloudrun.py). Firestore guarda lo que tiene que
# sobrevivir entre peticiones — el contenedor no conserva nada; PyJWT firma el
# JWT de la GitHub App, que es lo que se canjea por el token de instalación.
google-cloud-firestore>=2.11
pyjwt[crypto]>=2.8
